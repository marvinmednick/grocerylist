---
name: review-plan
description: Review an implementor's implementation plan against its spec. Use when the implementor has written a plan file and it needs to be checked for completeness and precision before implementation proceeds. Input is a feature ID (e.g. F1).
---

# Review Plan

Review an implementor's implementation plan against its spec.

Read and follow the full instructions in `commands/review-plan.md`.

Where the instructions reference `$ARGUMENTS`, use the feature ID the user specified.
