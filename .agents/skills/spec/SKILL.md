---
name: spec
description: Produce a structured implementation spec for a feature. Use when asked to write a spec, create implementation instructions, or prepare a feature for handoff to an implementor. Input is a feature name or F-number (e.g. F12, Smart Entry Model).
---

# Spec

Produce a structured implementation spec for a feature.

Read and follow the full instructions in `commands/spec.md`.

Where the instructions reference `$ARGUMENTS`, use the feature name or F-number the user specified.
