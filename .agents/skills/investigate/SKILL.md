---
name: investigate
description: Investigate a bug issue to understand its root cause without applying a fix. Use when a bug has been logged but the root cause is unknown and needs to be understood before deciding whether to fix. Input is a GitHub issue number (e.g. 18).
---

# Investigate

Investigate a bug issue to understand its root cause without fixing it.

Read and follow the full instructions in `commands/investigate.md`.

Where the instructions reference `$ARGUMENTS`, use the GitHub issue number the user specified.
