# known-test-failures.txt
#
# Pre-existing test failures that are acknowledged but not yet fixed.
# The check-tests script ignores these when checking for new regressions.
#
# Format:
#   [SUITE] <test-suite-name> | <test-name>
#   [SUITE_RUN_FAILURE] <test-suite-name>
#
# Remove entries when the underlying issue is fixed.
# Entries older than 30 days will be flagged as stale by check-tests.
#
# Added: 2026-02-24

# SmartAddItem is missing UndoProvider wrapper in its test setup.
# The component calls useUndo() which requires UndoProvider in the tree.
# Fix: wrap the render call in SmartAddItem-test.tsx with <UndoProvider>.
[SUITE] components/__tests__/SmartAddItem-test.tsx | SmartAddItem › renders the search input
